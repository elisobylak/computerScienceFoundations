/**
 * Auto Generated Java Class.
 */
import java.util.Scanner;
import java.io.*;

public class AirportReader {
  
  
  
  public static void main() throws IOException {
    String line;
    Scanner fscan = new Scanner(new File("airports.csv"));
    while (fscan.hasNext()) {
      line = fscan.nextLine();
      StringTokenizer tok = new StringTokenizer(line, ",");
      // read each token and print it on the same line separated by a space.
    }
  }
